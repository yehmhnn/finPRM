# FinPRM-Adapt proposal

Compile from this directory with:

```sh
latexmk -pdf finprm_proposal.tex
```

The manuscript intentionally contains no experimental results. Table 2 is a result template to be filled only after the evaluation is complete.

Before submission, replace `Authors to be inserted`, add the course/institution if required, and verify the exact model checkpoint and revision selected during implementation.
